	switch (t->back) {
	default: Uerror("bad return move");
	case  0: goto R999; /* nothing to undo */

		 /* PROC Q */
;
		;
		;
		
	case 4: // STATE 7
		goto R999;

	case 5: // STATE 8
		;
		now.turn = trpt->bup.oval;
		;
		goto R999;

	case 6: // STATE 12
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC P */
;
		;
		;
		
	case 8: // STATE 7
		goto R999;
;
		;
		
	case 10: // STATE 12
		;
		p_restor(II);
		;
		;
		goto R999;
	}

